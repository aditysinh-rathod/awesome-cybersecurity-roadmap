# 🛠 Systemd

## Overview

Systemd manages services and the boot process on modern Linux systems.

---

# Commands

```bash
systemctl status ssh

systemctl start ssh

systemctl stop ssh

systemctl restart ssh

systemctl enable ssh

systemctl disable ssh
```

---

# Logs

```bash
journalctl

journalctl -u ssh
```
